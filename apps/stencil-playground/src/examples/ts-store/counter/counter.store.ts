import { createAtom } from "@ssv/tanstack.stencil-store";

// standalone atoms — demonstrates createAtomCtrl usage
export const countAtom = createAtom(0);
export const additionalAtom = createAtom(0);
