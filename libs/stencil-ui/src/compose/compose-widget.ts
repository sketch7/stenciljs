/** Base class for compose wrapper components. Signals to `ssv-compose` that this component handles data/event mapping itself. */
// eslint-disable-next-line no-extraneous-class
export class ComposeWidget {
	static readonly isComposeWrapper = true;
}
